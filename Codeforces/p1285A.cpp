#include<bits/stdc++.h>
using namespace std;

int main()
{
    freopen("input.txt", "r", stdin);
    int n, l, r;
    cin >> n;

    string str;

    cin >> str;

    l = abs((n + 1) / 2);

    int res = l + 1;

    cout << res << endl;

    return 0;
}
